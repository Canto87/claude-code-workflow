# Web Dashboard Vision

> **Status**: Future Feature
> **Priority**: Medium
> **Last Updated**: 2024-01-18

로컬 웹 대시보드로 기능 설계 및 구현 진행을 관리하는 시스템 비전.

## 목표

GitHub Projects / Trello와 같은 칸반 보드 형태로:
- 기능별 티켓 발행
- 페이즈별 진행 추적
- CLI 선택 화면을 웹에서 렌더링

## 시스템 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Web Dashboard                                │
│                      localhost:3000                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────┐ ┌────────────────────────────────┐│
│  │      📊 Kanban Board        │ │    💬 CLI Selection View      ││
│  │                             │ │                                ││
│  │  Backlog → Phase1 → Done    │ │  질문이 발생하면 여기에 표시   ││
│  │                             │ │  선택 후 CLI로 전달            ││
│  └─────────────────────────────┘ └────────────────────────────────┘│
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────────┐│
│  │                       📜 Progress Log                           ││
│  └─────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
```

## 핵심 기능

### 1. 칸반 보드

#### 전체 구조

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  📊 Feature Board                                        [+ 새 기능]       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │
│  │   📋 설계중    │ │   🚧 구현중    │ │   🔍 검토중   │ │   ✅ 완료     │   │
│  │   (Design)    │ │ (In Progress) │ │   (Review)   │ │    (Done)     │   │
│  ├───────────────┤ ├───────────────┤ ├───────────────┤ ├───────────────┤   │
│  │               │ │               │ │               │ │               │   │
│  │ ┌───────────┐ │ │ ┌───────────┐ │ │               │ │ ┌───────────┐ │   │
│  │ │payment    │ │ │ │user-auth  │ │ │               │ │ │analytics  │ │   │
│  │ │-system    │ │ │ │           │ │ │               │ │ │           │ │   │
│  │ └───────────┘ │ │ └───────────┘ │ │               │ │ └───────────┘ │   │
│  │               │ │               │ │               │ │               │   │
│  └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 기능 카드 (Feature Card)

```
┌─────────────────────────────────────┐
│ 💳 payment-system                   │
├─────────────────────────────────────┤
│                                     │
│ 목표: 결제 처리 자동화              │
│                                     │
│ Phase 진행률:                       │
│ ┌─────────────────────────────────┐ │
│ │ P1 ████████░░ 80%               │ │
│ │ P2 ██░░░░░░░░ 20%               │ │
│ │ P3 ░░░░░░░░░░  0%               │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 🏷️ Clean Architecture | PostgreSQL │
│ ⚠️ Risk: Medium                     │
│                                     │
│ 📅 Updated: 2시간 전                │
└─────────────────────────────────────┘
```

#### 기능 카드 상세 보기 (클릭 시)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  💳 payment-system                                              [X 닫기]   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  📌 기본 정보                                                               │
│  ├── 목표: 결제 처리 자동화                                                │
│  ├── 아키텍처: Clean Architecture                                          │
│  ├── 저장소: PostgreSQL                                                    │
│  └── 패턴: Async Worker                                                    │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│                                                                             │
│  📋 Phase 상세                                                              │
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                         │
│  │  Phase 1    │  │  Phase 2    │  │  Phase 3    │                         │
│  │  Foundation │  │  Core API   │  │  Advanced   │                         │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤                         │
│  │ ✅ 데이터    │  │ ⏳ 결제API  │  │ ⬚ 환불     │                         │
│  │    모델     │  │             │  │             │                         │
│  │ ✅ 기본     │  │ ⬚ 웹훅     │  │ ⬚ 리포트   │                         │
│  │    설정     │  │             │  │             │                         │
│  │ ⏳ 검증     │  │             │  │             │                         │
│  │    로직     │  │             │  │             │                         │
│  └─────────────┘  └─────────────┘  └─────────────┘                         │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│                                                                             │
│  🎯 Actions                                                                 │
│  [설계 계속하기]  [Phase 1 구현 시작]  [문서 보기]  [삭제]                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### Phase 보드 (기능 내부 상세)

기능 카드 클릭 → Phase 선택 시:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  💳 payment-system > Phase 1: Foundation                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │
│  │   📋 대기     │ │   🚧 진행중    │ │   🔍 리뷰     │ │   ✅ 완료     │   │
│  ├───────────────┤ ├───────────────┤ ├───────────────┤ ├───────────────┤   │
│  │               │ │               │ │               │ │               │   │
│  │ ┌───────────┐ │ │ ┌───────────┐ │ │               │ │ ┌───────────┐ │   │
│  │ │테스트     │ │ │ │검증 로직  │ │ │               │ │ │데이터     │ │   │
│  │ │코드 작성  │ │ │ │구현       │ │ │               │ │ │모델 정의  │ │   │
│  │ └───────────┘ │ │ └───────────┘ │ │               │ │ └───────────┘ │   │
│  │               │ │               │ │               │ │ ┌───────────┐ │   │
│  │               │ │               │ │               │ │ │기본 설정  │ │   │
│  │               │ │               │ │               │ │ │파일 생성  │ │   │
│  │               │ │               │ │               │ │ └───────────┘ │   │
│  │               │ │               │ │               │ │               │   │
│  └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 티켓 (Task Card)

```
┌───────────────────────┐
│ 📝 검증 로직 구현      │
├───────────────────────┤
│ Phase 1 > Task 3      │
│                       │
│ 파일:                 │
│ • validator.go        │
│ • validator_test.go   │
│                       │
│ ⏱️ 예상: 2시간         │
│ ⚠️ 의존: Task 1, 2     │
└───────────────────────┘
```

#### 상태 흐름

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Feature 상태 흐름                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   [+ 새 기능]                                                               │
│        │                                                                    │
│        ▼                                                                    │
│   ┌─────────┐    plan-feature     ┌─────────┐    init-impl    ┌─────────┐ │
│   │  설계중  │ ──────완료───────► │  구현중  │ ────완료─────► │  검토중  │ │
│   └─────────┘                     └─────────┘                 └─────────┘ │
│        │                               │                           │       │
│        │ 설계 취소                      │ 구현 취소                  │       │
│        ▼                               ▼                           ▼       │
│   ┌─────────┐                    ┌─────────┐                 ┌─────────┐  │
│   │  취소됨  │                    │  취소됨  │                 │   완료   │  │
│   └─────────┘                    └─────────┘                 └─────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 보드 데이터 구조

```yaml
# .plan-feature/payment-system/board.yaml
feature:
  name: payment-system
  status: in_progress  # design | in_progress | review | done
  goal: 결제 처리 자동화

  meta:
    architecture: clean-architecture
    storage: postgresql
    pattern: async-worker
    risk: medium

  phases:
    - id: phase-1
      name: Foundation
      status: in_progress
      progress: 80
      tasks:
        - id: task-1
          name: 데이터 모델 정의
          status: done
        - id: task-2
          name: 기본 설정 파일
          status: done
        - id: task-3
          name: 검증 로직 구현
          status: in_progress

    - id: phase-2
      name: Core API
      status: pending
      progress: 0
      tasks:
        - id: task-1
          name: 결제 API
          status: pending
```

#### 인터랙션

| 동작 | 결과 |
|------|------|
| 카드 드래그 | 상태 변경 (board.yaml 업데이트) |
| 카드 클릭 | 상세 보기 모달 |
| [설계 계속하기] | CLI 선택 화면으로 전환 |
| [구현 시작] | `/impl` 명령 실행 |
| [문서 보기] | 마크다운 문서 표시 |

### 2. CLI 선택 화면 렌더링

CLI에서 질문이 발생하면 웹에서 시각적으로 표시:

```
┌───────────────────────────────────────────────────────────────┐
│  Step 4: Architecture Q&A                                     │
│                                                               │
│  Claude:                                                      │
│  "어떤 저장소를 사용할까요?                                    │
│   데이터 특성과 확장성을 고려해서 선택해주세요."               │
│                                                               │
│  ┌─────────────────┐  ┌─────────────────┐                    │
│  │ PostgreSQL      │  │ MongoDB         │                    │
│  │ 관계형 데이터    │  │ 문서형 데이터    │                    │
│  │ 트랜잭션 지원    │  │ 유연한 스키마    │                    │
│  │                 │  │                 │                    │
│  │    [선택]       │  │    [선택]       │                    │
│  └─────────────────┘  └─────────────────┘                    │
│                                                               │
│  💬 Other: [직접 입력...                    ] [전송]          │
└───────────────────────────────────────────────────────────────┘
```

### 3. 진행 로그

```
📜 진행 로그
├── ✅ Step 1: Config loaded
├── ✅ Step 2: Feature name: payment-system
├── ✅ Step 3: Codebase analyzed (Clean Architecture)
└── ⏳ Step 4: Waiting for storage selection...
```

## 기술 아키텍처

### 컴포넌트

```
┌──────────┐                  ┌──────────┐                 ┌──────────┐
│   Web    │   WebSocket      │  Bridge  │   stdin/out    │  Claude  │
│    UI    │◄────────────────►│  Server  │◄──────────────►│   CLI    │
└──────────┘                  └──────────┘                 └──────────┘
                                   │
                                   ▼
                              출력 파싱
                              (질문 감지)
                                   │
                                   ▼
                              파일 감시
                              (상태 변경)
```

### 기술 스택

| 컴포넌트 | 기술 |
|----------|------|
| Frontend | HTML + Vanilla JS (또는 React/Vue) |
| Backend | Node.js + Express |
| WebSocket | ws 라이브러리 |
| PTY | node-pty |
| 파일 감시 | chokidar |

### 디렉토리 구조

```
project-dashboard/
├── server/
│   ├── index.js          # Express + WebSocket 서버
│   ├── terminal.js       # PTY 관리 (Claude CLI 실행)
│   ├── parser.js         # CLI 출력 파싱 (질문 감지)
│   └── watcher.js        # 파일 변경 감시
├── client/
│   ├── index.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       ├── dashboard.js  # 칸반 보드
│       ├── selector.js   # CLI 선택 화면 렌더링
│       └── log.js        # 진행 로그
└── package.json
```

## 데이터 흐름

### 질문 발생 시

```
1. Claude CLI 실행 중
2. AskUserQuestion 호출 → CLI 출력
3. Bridge Server가 출력 파싱
4. 질문 패턴 감지 (options, multiSelect 등)
5. WebSocket으로 웹 UI에 전송
6. 웹에서 선택 화면 렌더링
7. 사용자 선택
8. WebSocket으로 Bridge Server에 전송
9. Bridge Server → CLI stdin으로 전달
10. CLI 계속 진행
```

### 상태 변경 시

```
1. CLI가 상태 파일 업데이트 (.plan-feature/*/state.md)
2. chokidar가 파일 변경 감지
3. 새 상태 파싱
4. WebSocket으로 웹 UI에 전송
5. 칸반 보드 자동 업데이트
```

## 선택 화면 타입

| 타입 | CLI 패턴 | 웹 렌더링 |
|------|----------|----------|
| 단일 선택 | `multiSelect: false` | 라디오 버튼 / 카드 |
| 다중 선택 | `multiSelect: true` | 체크박스 |
| 텍스트 입력 | `Other` 옵션 | 입력 필드 |
| 확인/취소 | 2개 옵션 | 버튼 2개 |
| 프리뷰 | 마크다운 출력 | 마크다운 렌더링 + 승인/수정 |

## CLI 출력 파싱 예시

```javascript
// parser.js
function parseOutput(data) {
  // AskUserQuestion JSON 패턴 감지
  const questionMatch = data.match(/\{[\s\S]*"questions"[\s\S]*\}/);
  if (questionMatch) {
    try {
      const parsed = JSON.parse(questionMatch[0]);
      return {
        type: 'question',
        data: parsed.questions[0]
      };
    } catch (e) {}
  }

  // 일반 출력
  return { type: 'log', content: data };
}
```

## 스킬 연동

### plan-feature 연동

```
/plan-feature payment-system
    ↓
웹 대시보드에 새 기능 카드 생성
    ↓
설계 진행 (질문은 웹에서 선택)
    ↓
완료 시 티켓 자동 생성
```

### init-impl 연동

```
/impl payment-system phase-1
    ↓
해당 Phase 카드 상태 변경 (In Progress)
    ↓
구현 진행
    ↓
완료 시 카드 이동 (Done)
```

## 구현 단계

### Phase 1: 기본 구조

- [ ] Express 서버 설정
- [ ] WebSocket 연결
- [ ] node-pty로 Claude CLI 실행
- [ ] 기본 출력 표시

### Phase 2: 질문 렌더링

- [ ] CLI 출력 파싱
- [ ] 질문 감지 로직
- [ ] 선택 화면 렌더링
- [ ] 선택 결과 CLI 전달

### Phase 3: 칸반 보드

- [ ] 상태 파일 감시
- [ ] 칸반 보드 UI
- [ ] 드래그 앤 드롭 (선택)
- [ ] 티켓 상세 보기

### Phase 4: 통합

- [ ] plan-feature 완전 연동
- [ ] init-impl 연동
- [ ] 진행률 시각화
- [ ] 히스토리 조회

## 참고

- 기존 CLI 시스템과 완전 호환
- 웹 없이도 CLI만으로 동작 가능
- 웹은 선택적 시각화 레이어
